<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      x:Class="LifeBoard.Views.Issues.EditIssueView" 
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800"
	Title="EditIssueView">
    <Page.Resources>
        <DataTemplate x:Key="AddItemDataTemplate">
            <Grid>
                <TextBlock Text="{Binding IssueType}" Height="30" HorizontalAlignment="Left" Margin="40,-7,0,0" Style="{DynamicResource GroupTextStyle}"/>
                <TextBlock Text="{Binding Summary}" Height="30" HorizontalAlignment="Left" Margin="90,7,0,0" Style="{DynamicResource SubtitleTextStyle}"/>
                <Button Content="{StaticResource AddParentHeader}" Command="{Binding Parent.AddCommand}" CommandParameter="{Binding}" Width="30" Height="30" HorizontalAlignment="Left" Style="{DynamicResource ButtonStyle}" />
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="RemoveItemDataTemplate">
            <Grid>
                <TextBlock Text="{Binding IssueType}" Height="30" HorizontalAlignment="Left" Margin="40,-7,0,0" Style="{DynamicResource GroupTextStyle}"/>
                <TextBlock Text="{Binding Summary}" Height="30" HorizontalAlignment="Left" Margin="90,7,0,0" Style="{DynamicResource SubtitleTextStyle}"/>
                <Button Content="{StaticResource RemoveParentHeader}" Command="{Binding Parent.RemoveCommand}" CommandParameter="{Binding}" Width="30" Height="30" HorizontalAlignment="Left" Style="{DynamicResource ButtonStyle}" />
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <Button Content="{Binding SubmitHeader}" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Command="{Binding SubmitCommand}" Style="{DynamicResource MainButtonStyle}" Grid.Column="1"/>
        <Button HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding Parent.BackCommand}" Style="{DynamicResource BackButtonStyle}"/>
        <ScrollViewer Margin="0,60,0,0" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox Height="25" Margin="15,51,20,0" TextWrapping="Wrap" Text="{Binding Summary}" VerticalAlignment="Top" Grid.Column="1" Style="{DynamicResource TitleTextBoxStyle}"/>
                <TextBox Height="150" Margin="15,144,20,0" TextWrapping="Wrap" Text="{Binding Description}" VerticalAlignment="Top" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Grid.Column="1" Style="{DynamicResource SubtitleTextBoxStyle}"/>
                <TextBlock Text="{StaticResource SummaryLabel}" HorizontalAlignment="Left" Margin="48,44,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}"/>
                <TextBlock Text="{StaticResource DescriptionLabel}" HorizontalAlignment="Left" Margin="48,137,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}" />
                <TextBlock Text="{StaticResource TypeLabel}" HorizontalAlignment="Left" Margin="48,0,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}"/>
                <ComboBox HorizontalAlignment="Left" Margin="15,8,0,0" VerticalAlignment="Top" Width="150" ItemsSource="{Binding Types}" SelectedValue="{Binding Type}" Grid.Column="1"/>
                <TextBlock Text="{StaticResource PriorityLabel}" HorizontalAlignment="Left" Margin="48,90,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}"/>
                <ComboBox HorizontalAlignment="Left" Margin="15,98,0,0" VerticalAlignment="Top" Width="150" ItemsSource="{Binding Priorities}" SelectedValue="{Binding Priority}" Grid.Column="1"/>
                <Grid Grid.Column="1" Margin="15,317,20,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="{StaticResource TypesLabel}" VerticalAlignment="Top" Style="{DynamicResource GroupTextStyle}"/>
                    <TextBlock HorizontalAlignment="Left" Margin="0,145,0,0" TextWrapping="Wrap" Text="{StaticResource StatusesLabel}" VerticalAlignment="Top" Style="{DynamicResource GroupTextStyle}"/>
                    <Rectangle Fill="{StaticResource SelectedBrush7}" HorizontalAlignment="Stretch" Height="1" Margin="0,32,0,0" VerticalAlignment="Top"/>
                    <Rectangle Fill="{StaticResource SelectedBrush7}" HorizontalAlignment="Stretch" Height="1" Margin="0,177,0,0" VerticalAlignment="Top"/>
                    <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" Height="100" HorizontalAlignment="Stretch" Margin="5,35,5,0" VerticalAlignment="Top" ItemsSource="{Binding Filter.Types}" ItemTemplate="{DynamicResource FilterItemDataTemplate}" BorderThickness="0" />
                    <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" Height="100" HorizontalAlignment="Stretch" Margin="5,180,5,0" VerticalAlignment="Top" ItemsSource="{Binding Filter.Statuses}" ItemTemplate="{DynamicResource FilterItemDataTemplate}" BorderThickness="0" />
                    <ListBox Grid.Column="1" Margin="10,10,10,0" ItemTemplate="{DynamicResource AddItemDataTemplate}" ItemsSource="{Binding Issues}" Height="300" VerticalAlignment="Top"/>
                    <ListBox Grid.Column="2" Margin="10,10,10,0" ItemsSource="{Binding ParentIssues}" ItemTemplate="{DynamicResource RemoveItemDataTemplate}" Height="300" VerticalAlignment="Top"/>
                </Grid>
                <TextBlock Text="{StaticResource ParentsLabel}" HorizontalAlignment="Left" Margin="48,317,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
