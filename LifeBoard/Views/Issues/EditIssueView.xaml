<Page x:Class="LifeBoard.Views.Issues.EditIssueView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
	Title="EditIssueView">

    <Grid>
        <Button Content="Edit" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Command="{Binding EditCommand}" Style="{DynamicResource MainButtonStyle}"/>
        <Button HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding BackNavigateCommand}" Style="{DynamicResource BackButtonStyle}"/>
        <TextBox Height="25" Margin="165,144,20,0" TextWrapping="Wrap" Text="{Binding Summary}" VerticalAlignment="Top"/>
        <TextBox Height="150" Margin="165,237,20,0" TextWrapping="Wrap" Text="{Binding Description}" VerticalAlignment="Top" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        <TextBlock Text="Summary:" HorizontalAlignment="Left" Margin="48,137,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}"/>
        <TextBlock Text="Description:" HorizontalAlignment="Left" Margin="48,230,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}" />
        <TextBlock Text="Type:" HorizontalAlignment="Left" Margin="48,93,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}"/>
        <ComboBox HorizontalAlignment="Left" Margin="165,101,0,0" VerticalAlignment="Top" Width="150" ItemsSource="{Binding Types}" SelectedValue="{Binding Type}"/>
        <TextBlock Text="Priority:" HorizontalAlignment="Left" Margin="48,183,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}"/>
        <ComboBox HorizontalAlignment="Left" Margin="165,191,0,0" VerticalAlignment="Top" Width="150" ItemsSource="{Binding Priorities}" SelectedValue="{Binding Priority}"/>
    </Grid>
</Page>
