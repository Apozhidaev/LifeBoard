<Page x:Class="LifeBoard.Views.Issues.ShowIssueView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
	Title="ShowIssueView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Button HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding BackNavigateCommand}" Style="{DynamicResource BackButtonStyle}"/>
        <TextBlock Height="23" Margin="200,80,20,0" TextWrapping="Wrap" Text="{Binding Summary}" VerticalAlignment="Top" Style="{DynamicResource TitleTextStyle}" />
        <TextBlock Height="150" Margin="200,125,20,0" TextWrapping="Wrap" Text="{Binding Description}" VerticalAlignment="Top" Style="{DynamicResource SubtitleTextStyle}"/>
        <TextBlock Text="Summary:" HorizontalAlignment="Left" Margin="100,73,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}" />
        <TextBlock Text="Description:" HorizontalAlignment="Left" Margin="100,118,0,0" VerticalAlignment="Top" Style="{DynamicResource SubheaderTextStyle}" />
        <TextBlock Grid.Column="1" Text="Type:" HorizontalAlignment="Left" Margin="10,73,0,0" VerticalAlignment="Top" Style="{DynamicResource GroupTextStyle}" />
        <TextBlock Grid.Column="1" Text="Status:" HorizontalAlignment="Left" Margin="10,118,0,0" VerticalAlignment="Top" Style="{DynamicResource GroupTextStyle}" />
        <TextBlock Grid.Column="1" Text="{Binding IssueType}" HorizontalAlignment="Left" Margin="100,81,0,0" VerticalAlignment="Top" Style="{DynamicResource TitleTextStyle}" />
        <TextBlock Grid.Column="1" Text="{Binding Status}" HorizontalAlignment="Left" Margin="100,125,0,0" VerticalAlignment="Top" Style="{DynamicResource TitleTextStyle}" />
        <Rectangle Grid.Column="1" Fill="#FF1E395B" HorizontalAlignment="Stretch" Height="1" Margin="10,108,20,0" VerticalAlignment="Top"/>
        <Rectangle Grid.Column="1" Fill="#FF1E395B" HorizontalAlignment="Stretch" Height="1" Margin="10,149,20,0" VerticalAlignment="Top"/>
        <TextBlock Grid.Column="1" Text="Priority:" HorizontalAlignment="Left" Margin="10,159,0,0" VerticalAlignment="Top" Style="{DynamicResource GroupTextStyle}" />
        <TextBlock Grid.Column="1" Text="{Binding Priority}" HorizontalAlignment="Left" Margin="100,166,0,0" VerticalAlignment="Top" Style="{DynamicResource TitleTextStyle}" />
        <Rectangle Grid.Column="1" Fill="#FF1E395B" HorizontalAlignment="Stretch" Height="1" Margin="10,190,20,0" VerticalAlignment="Top"/>
        <Button Content="{Binding NextStatus}" Grid.Column="1" HorizontalAlignment="Right" Margin="0,10,10,0" VerticalAlignment="Top" Command="{Binding NextStateCommand}" Style="{DynamicResource MainButtonStyle}"/>
    </Grid>
</Page>
