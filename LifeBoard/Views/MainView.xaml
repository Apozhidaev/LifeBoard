<Window x:Class="LifeBoard.Views.MainView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Life Board" WindowState="Maximized" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="3" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu HorizontalAlignment="Stretch" Margin="0" VerticalAlignment="Stretch" FontSize="16" Style="{DynamicResource MenuStyle}" Background="White">
            <MenuItem Header="Dashboard" IsChecked="{Binding DashboardPage.IsChecked,Mode=OneWay}" Command="{Binding DashboardPage.NavigateCommand}" Width="150" Height="50" Template="{DynamicResource MenuItemControlTemplate}" />
            <MenuItem Header="Issues" IsChecked="{Binding IssuesPage.IsChecked,Mode=OneWay}"  Command="{Binding IssuesPage.NavigateCommand}" Width="150" Height="50" Template="{DynamicResource MenuItemControlTemplate}"/>
            <MenuItem Header="Configuration" IsChecked="{Binding ConfigurationPage.IsChecked,Mode=OneWay}"  Command="{Binding ConfigurationPage.NavigateCommand}" Width="150" Height="50" Template="{DynamicResource MenuItemControlTemplate}"/>
        </Menu>
        <Rectangle Height="3" Fill="#FF1E395B" VerticalAlignment="Stretch" Grid.Row="1" />
        <Frame NavigationUIVisibility="Hidden" Content="{Binding Current.Page}" HorizontalAlignment="Stretch" Margin="0" Grid.Row="2" VerticalAlignment="Stretch" />
    </Grid>
</Window>